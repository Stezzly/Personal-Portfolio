/**
 * Global registry for CLI demo configs. Each per-demo script adds its config here.
 * Keys: demo id (kebab-case). Values: { id, title, modalTitle, description, repoUrl, steps }.
 */
var CLI_DEMOS = {};
